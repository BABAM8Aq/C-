#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
    int answer;
    int num;
    int count=0;

    srand(time(NULL));

    answer = rand() % 100 + 1;

    printf("1부터 100사이에 숫자를 맞춰보세요!");

    while(1)
    {
        printf("숫자 입력:")
    }
}








    printf("1부터 100사이에 숫자를 맞춰보세요!");

    while(1)
    {
        printf("숫자 입력:")}